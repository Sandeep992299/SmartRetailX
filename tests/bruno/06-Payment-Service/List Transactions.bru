meta {
  name: List Transactions Admin
  type: http
  seq: 1
}

get {
  url: {{GATEWAY_URL}}/payments/transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

assert {
  res.status: eq 200
  res.body: isDefined
}
