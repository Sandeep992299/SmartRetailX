meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{GATEWAY_URL}}/users/login
  body: json
  auth: none
}

body:json {
  {
    "email": "bruno_test@smartretailx.com",
    "password": "BrunoPassword123!"
  }
}

vars:post-response {
  token: res.body.access_token
}
