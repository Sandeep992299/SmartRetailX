meta {
  name: List Products
  type: http
  seq: 1
}

get {
  url: {{GATEWAY_URL}}/products
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body: isDefined
}

vars:post-response {
  product_id: res.body[0].id
}
