meta {
  name: Get Product
  type: http
  seq: 2
}

get {
  url: {{GATEWAY_URL}}/products/{{product_id}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: isDefined
  res.body.name: isDefined
}
