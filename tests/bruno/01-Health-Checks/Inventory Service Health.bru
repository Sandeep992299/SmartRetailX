meta {
  name: Inventory Service Health
  type: http
  seq: 6
}

get {
  url: {{GATEWAY_URL}}/inventory/healthz
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.status: eq healthy
}
